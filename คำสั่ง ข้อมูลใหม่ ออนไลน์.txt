git config --global alias.pushall "!f() { \
  git add -A && \
  if ! git diff --cached --quiet; then \
    git commit -m 'อัปเดตโค้ดและ Excel'; \
  fi; \
  git pull origin main --rebase; \
  git push origin main; \
}; f"
